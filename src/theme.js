import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "#F2F2F2",
    },
    primary: {
      main: "#FFA321",
    },
    secondary: {
      main: "#262626",
    },
    text: {
      primary: "#1D3944",
      secondary: "#999999",
    },
  },
});

export default theme;
